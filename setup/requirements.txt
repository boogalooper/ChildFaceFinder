# Runtime dependencies installed normally.
# InsightFace itself is installed separately by install.ps1 with --no-deps,
# because its metadata asks for CPU onnxruntime + GUI opencv-python. This
# project deliberately uses onnxruntime-gpu + opencv-python-headless instead.
onnxruntime-gpu[cuda]==1.26.0
# Pin the CUDA 12.8 component wheels too: ORT's [cuda] extra otherwise uses
# compatible ranges and a reinstall months later can resolve a different stack.
nvidia-cuda-nvrtc-cu12==12.8.61
nvidia-cuda-runtime-cu12==12.8.57
nvidia-cublas-cu12==12.8.3.14
nvidia-cufft-cu12==11.3.3.41
nvidia-curand-cu12==10.3.9.55
nvidia-nvjitlink-cu12==12.8.61
nvidia-cudnn-cu12==9.7.1.26
numpy==1.26.4
onnx==1.18.0
opencv-python-headless==4.11.0.86
scipy==1.14.1
scikit-image==0.25.2
tqdm==4.67.1
requests==2.32.3
Pillow==12.3.0
pillow-heif==1.5.0
rawpy==0.27.0
